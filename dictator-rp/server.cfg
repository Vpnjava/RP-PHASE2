// Arma Reforger Server Configuration for Dictator RP
// Place this file in your server root directory as server.cfg

// Basic server settings
hostname = "Dictator Island RP [AR] - Phase 2";
password = "";
passwordAdmin = "your_admin_password_here";
maxPlayers = 64;

// Network settings
ip = "";
port = 2001;
publicPort = 2001;
upnp = true;

// Game settings
gameHostBindAddress = "";
gameHostBindPort = 2001;
gameHostRegisterBindAddress = "";
gameHostRegisterPort = 2001;

// Mission and world configuration
// Using default Everon world - no custom world needed
scenario = "{ECC61978EDCC2B5A}Missions/23_Campaign.conf";

// Mods configuration - Add Dictator RP mod here
mods[] = {
    "dictator-rp"  // Your mod folder name
};

// Server behavior
serverMaxViewDistance = 3000;
serverMinGrassDistance = 50;
networkViewDistance = 1000;

// Admin and moderation
autoInit = true;
persistent = true;
missionsToServerRestart = 1;
restartServerAfterMissionsCount = 3;

// Performance settings for console compatibility
forcedDifficulty = "REGULAR";
battlEye = true;

// Logging
logStats = 60;
logPlayers = true;
logFile = "server.log";

// Region and time settings
region = "ME";  // Middle East
timeZone = "UTC+3";

// Game rules specific to RP
disableVoN = false;  // Keep voice chat enabled for RP
vonCodec = 1;
vonCodecQuality = 7;

// Anti-cheat and security
verifySignatures = 2;  // Verify signatures strictly
allowedFilePatching = 1;

// Headless client settings (optional)
headlessClients[] = {};
localClient[] = {};

// Server description visible in browser
serverDescription = "Arabic Roleplay Server - Dictator Island RP\n\nFeatures:\n- Full Arabic UI (RTL)\n- Police & EMS Systems\n- Economy & Jobs\n- Phone System v3\n- Anti-Cheat Protection\n\nJoin our Discord: [Your Discord Link]";

// Additional RP-specific settings
disableCrossTalk = true;  // Prevent channel hopping
enablePlayerDiag = false;  // Disable debug overlay
allowProfileGlasses = true;
allowProfileGoggles = true;
